<div class="container">
	<div class="row">
		<div class="col-sm-3">

<%= render 'new/new',book:@book,user:@user %>
		</div>

<div class="col-sm-9">
<h2> Books2</h2>
<table class="table table-striped">
	<thead>
		<tr>
			<th></th>
			<th>Title</th>
			<th>Opinion</th>
		</tr>
	</thead>

	<tbody>
		<% @books.each do |book| %>
		<tr>
			<td><%= attachment_image_tag book.user,:profile_image,:fill, 100, 100,fallback:"no-image.png",size: '100x100' %></td>
			<td><%= link_to book.title,book_path(book) %></td>
			<td><%= book.body %></td>
		</tr>
		<% end %>
	</tbody>
</table>
</div>
	</div>
</div>
